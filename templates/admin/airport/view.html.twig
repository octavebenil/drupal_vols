<div class="title">
  <span style="float: right;">
    <a class="btn btn-primary" href="{{ path('vols.airport_list') }}">Retour</a>
  </span>
</div>

<div class="table-responsive">
  <table class="table table-hover">
    <tbody>
        <tr>
          <td>ID</td><td> : </td><td>{{ airport.id }}</td>
        </tr>
        <tr>
          <td>CODE</td><td> : </td><td>{{ airport.code }}</td>
        </tr>

        <tr>
          <td>NOM</td><td> : </td><td>{{ airport.name }}</td>
        </tr>

        <tr>
          <td>Ajouter le</td><td> : </td><td>{{ airport.created_at|date('d/m/Y H:i:s') }}</td>
        </tr>


        <tr>
          <td>Modifier le</td><td> : </td><td>{{ airport.updated_at|date('d/m/Y H:i:s') }}</td>
        </tr>

    </tbody>
  </table>
</div>
