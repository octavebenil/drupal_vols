<nav class="top-nav">
  <h4 class="menu-title">MENU</h4>
  <div class="menu-sep"></div>

  <ul class="nav-list">

    <li class="nav-item {{ path('vols.vol_list') ==  path('<current>') ? 'active'}}">
      <a class="nav-link" href="{{ path('vols.vol_list') }}">Vols</a>
    </li>

    <li class="nav-item {{ path('vols.status_vols') ==  path('<current>') ? 'active' }}">
      <a class="nav-link" href="{{ path('vols.status_vols') }}">Statuts</a>
    </li>

    <li class="nav-item {{ path('vols.airport_list') ==  path('<current>') ? 'active' }}">
      <a class="nav-link" href="{{ path('vols.airport_list') }}">Aeroports</a>
    </li>

    <li class="nav-item {{ path('vols.company_list') ==  path('<current>') ? 'active' }}">
      <a class="nav-link" href="{{ path('vols.company_list') }}">Compagnies</a>
    </li>

  </ul>
</nav>
